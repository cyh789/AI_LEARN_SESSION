# 작업 요약 문서 (2026-03-07)

## 1) 생성한 파일
- `20260307`: 첫 번째 시 텍스트 파일
- `poem.md`: 마크다운 시 파일 (이후 3월 시로 업데이트)

## 2) Git/브랜치 작업
- 초기 커밋 생성 후 원격 저장소(`cyh789/ai_260307`)에 푸시
- 기본 개발 브랜치를 `master`에서 `main`으로 전환
- 원격 기본 브랜치도 `main`으로 변경
- 원격 `master` 브랜치 삭제 완료
- PR 기반 작업을 위해 `developer` 브랜치 생성 및 원격 추적 설정

## 3) 커밋/푸시 이력(주요)
- `b9ee446`: `20260307` 추가
- `9b4b8e1`: `poem.md` 추가
- `ded3f27`: `poem.md` 내용(3월 시) 업데이트

## 4) PR 작업
- `developer -> main` PR 생성: https://github.com/cyh789/ai_260307/pull/1
- PR 제목/내용 수정 요청 반영
  - 제목: `20260307 pr 제목`
  - 내용: `20260307 pr 내용`
- 한글 깨짐 이슈는 UTF-8 바이트로 재요청하여 정상 반영 확인

## 5) 환경 설정 작업
- GitHub CLI 설치 완료 (`gh 2.87.3`)
- PowerShell UTF-8 고정 설정 적용
  - 프로필 파일 생성/수정
  - 코드페이지 65001 및 입출력 인코딩 UTF-8 설정
- 실행 정책 이슈 해결을 위해 `CurrentUser` 범위 `RemoteSigned` 적용

## 6) 이슈 및 해결
- `gh auth` 토큰 무효 반복 발생
  - 일부 GitHub 작업은 `git credential` 기반 API 호출로 우회 처리
- 콘솔에서 한글이 `??`로 보이는 문제
  - 인코딩 설정 + UTF-8 전송으로 해결

## 7) 현재 상태
- 현재 작업 브랜치: `developer`
- 원격 기본 브랜치: `main`
- 원격 브랜치 구성: `main`, `developer`
- 작업 트리는 최근 커밋/푸시 기준 정상 운영 상태
